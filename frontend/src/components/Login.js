import React from "react";

const Login = () => {
  return (
    <div style={{ textAlign: "center", marginTop: "100px" }}>
      <h2>Login Page (placeholder)</h2>
      <p>Implement login form here (only verified users can log in).</p>
    </div>
  );
};

export default Login;
